// Input box has a "add" button
// Tasks can set priority (high, medium, low)
// Task box has a priority tag, and a "check" button
//    once the "check" button is clicked, the task box is deleted
// Responsive


// Fly in, fly out animation
// Categorized
// Re-order

const taskForm = document.querySelector('form');

taskForm.addEventListener('submit', evt => {

});
